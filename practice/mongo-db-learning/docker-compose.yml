version: '3.8'

services:
  mongodb-sandbox:
    image: mongo:6.0
    container_name: mongodb-sandbox
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=academic_sandbox
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: